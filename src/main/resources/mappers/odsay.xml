<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.direction.mapper.DirectionMapper">


    <insert id="direction">
        INSERT INTO direction(apiKey, lang, sx, sy, ex, ey)
        VALUES (#{apiKey}, #{lang}, #{sx}, #{sy}, #{ex}, #{ey});
    </insert>

    <insert id="odsayDirection">
        INSERT INTO search_pub (bus_count, subway_count, subway_bus_count, path_id)
        VALUES (#{bus_count}, #{subway_count}, #{subway_bus_count}, #{path_id});
    </insert>

    <insert id="odsayPath">
        INSERT INTO path_odsay (pathType, payment, totalTime)
        VALUES (#{pathType}, #{payment}, #{totalTime});
    </insert>

</mapper>